export default function DashboardPage() {
  return (
    <div className="rounded-2xl border border-brand-line bg-white/60 p-6 text-sm text-brand-ink/60">
      Signed-in dashboard — coming next (Dashboards &amp; Export phase).
    </div>
  );
}
